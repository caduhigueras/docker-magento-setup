[mysqld]
# Increase the innodb buffer pool size (adjust according to your needs)
innodb_buffer_pool_size = 1G
innodb_log_file_size = 256M

# Other recommended MariaDB settings
innodb_flush_log_at_trx_commit = 1
innodb_file_per_table = 1
max_connections = 200
